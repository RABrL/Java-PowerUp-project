server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_restaurant_plaza
    username: brito
    password: ${password}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
appDescription: "Pragma Powerup"
appVersion: "1.0.0"
